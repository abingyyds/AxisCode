[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'npx drizzle-kit migrate && node dist/index.js'"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
